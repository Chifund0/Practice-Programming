#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0
 * 
 */

int main(void)
{
	int length;
	int width;
	int area;

	length = 5;
	width = 8;
	
	printf("area = %d\n", length * width);
	return (0);
}
